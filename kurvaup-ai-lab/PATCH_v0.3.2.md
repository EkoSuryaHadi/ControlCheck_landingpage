# Patch v0.3.2

Fixes Vercel TypeScript build error caused by implicit `any` on Supabase SSR cookie callbacks.

Updated:
- `lib/supabase/server.ts`
- `middleware.ts`
- package version to `0.3.2`

After replacing the GitHub source, commit to `main` and let Vercel redeploy.
